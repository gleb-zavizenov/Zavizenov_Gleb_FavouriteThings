const config = {
    host: "localhost",
    port: 8889, // 8889 for mac, empty for windows
    uname: "root",
    pword: "root", // leave blank for windows
    database: "db_card"
}

module.exports = config;